@import "/assets/iconfont/iconfont.wxss";

/**app.wxss**/
.container {
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: space-between;
  padding: 200rpx 0;
  box-sizing: border-box;
} 
page{
  /* 全局定义主题颜色 */
  --themeColor:red;
  --tipColor:rgb(197, 21, 109);
    /* 定义统一字体大小 */
  font-size:28rpx;
}